apply plugin: 'com.android.library'

def safeExtGet(prop, fallback) {
    rootProject.ext.has(prop) ? rootProject.ext.get(prop) : fallback
}


android {
    compileSdkVersion safeExtGet("compileSdkVersion", 25)
    buildToolsVersion safeExtGet("buildToolsVersion", '25.0.2')

    defaultConfig {
        minSdkVersion safeExtGet('minSdkVersion', 16)
        targetSdkVersion safeExtGet('targetSdkVersion', 25)
        versionCode 1
        versionName "1.0"
    }

}

dependencies {
    implementation "com.facebook.react:react-native:+"
//    implementation "com.android.support:support-v4:${safeExtGet("supportLibVersion", "27.0.2")}"
//    implementation "com.android.support:support-annotations:${safeExtGet("supportLibVersion", "27.0.2")}"
    implementation 'com.scwang.smartrefresh:SmartRefreshLayout:1.1.0'
    implementation fileTree(dir: 'libs', include: ['*.jar'])
}
